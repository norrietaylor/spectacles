---
on:
  workflow_dispatch:
permissions:
  contents: read
engine: claude
---

# Sample agentic workflow

This placeholder agentic workflow exists so `gh aw compile` has a target
during repository bootstrap. The real SDD agents replace it in later units.

When run, reply with `ok` and take no further action.
