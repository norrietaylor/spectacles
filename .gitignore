# macOS
.DS_Store

# Claude Code agent worktrees and local config
.claude/

# mkdocs build output
site/

# gh-aw vendored import cache. `gh aw compile` regenerates this tree; with
# inlined-imports the compiled .lock.yml workflows are self-contained and need
# no committed copy of it (ADR 0004).
.github/aw/imports/

# gh-aw audit artifacts. `gh aw audit <run-id>` downloads run logs here.
.github/aw/logs/
